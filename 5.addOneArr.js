// Add one to each number in number array
const addOneArr = (numberArr) => {
  return numberArr.map((numberArr) => numberArr + 1);
};
let numberArr = [1, 2, 3, 4, 5];
console.log(addOneArr(numberArr));

// test passed 